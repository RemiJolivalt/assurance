SELECT
  *
FROM
  public.garanties_utilisateur
ORDER BY
  "{{Table2.sortOrder.column || 'id_garantie'}}" {{Table2.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{Table2.pageSize}}
OFFSET
  {{Table2.pageOffset}}